# AgentUnity — Configuration Pod RunPod
# Copiez ce fichier en .env et adaptez les valeurs

# Mode: "local" ou "pod"
RUN_MODE=pod

# API
API_SECRET_KEY=your-secret-key-here

# MCP-Unity (via tunnel cloudflared)
# Lancez cloudflared: cloudflared tunnel --url ws://localhost:8090
# Puis mettez l'URL wss://xxx.trycloudflare.com/McpUnity ici
MCP_UNITY_WS_URL=wss://your-tunnel.trycloudflare.com/McpUnity

# vLLM (sur le pod RunPod)
# Remplacez PODID par votre ID de pod
VLLM_LLM_URL=https://PODID-8000.proxy.runpod.net/v1
VLLM_VISION_URL=https://PODID-8001.proxy.runpod.net/v1

# Embedding / Reranker (sur le pod)
EMBEDDING_URL=https://PODID-8002.proxy.runpod.net
RERANKER_URL=https://PODID-8003.proxy.runpod.net

# Qdrant (sur le pod)
QDRANT_URL=https://PODID-6333.proxy.runpod.net

# Pod connection
POD_ID=PODID